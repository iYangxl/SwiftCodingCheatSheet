# SwiftCodingCheatSheet

- String to Int
```
Int(str)
```

- Array as queue
```
arr.first, arr.last
let f = arr.removeFirst()
let e = arr.removeLast()
arr.insert(e, at: 0)
arr.append(e)
arr.isEmpty
```
